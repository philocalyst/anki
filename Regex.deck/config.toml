crowdanki_uuid = "aae2db35-a61c-4489-b3fd-e2a1c06149ea"
name = "regex"
camelCase = 100  # maxTaken - max cards per day, adjust as needed
autoplay = true
replayq = true
timer = 0  # 0 = timer off, or set to seconds
another_retreat = false

[deck_configurations.new]
delays = [1, 10]  # Learning steps in minutes
ints = [1, 4, 7]  # Graduating intervals in days
initial_factor = 2500  # Starting ease (2500 = 250%)
per_day = 20  # New cards per day
order = 1  # 0=sequential, 1=random
bury = true  # Bury related new cards
separate = false

[deck_configurations.rev]
per_day = 200  # Review cards per day
ease4 = 1.3  # Easy bonus multiplier
ivl_fct = 1.0  # Interval factor
fuzz = 0.05  # Fuzz factor (5%)
hard_factor = 1.2  # Hard interval multiplier
max_ivl = 36500  # Maximum interval in days (~100 years)
min_space = 1  # Minimum spacing in days
bury = true  # Bury related reviews

[deck_configurations.lapse]
delays = [10]  # Relearning steps in minutes
mult = 0.0  # New interval multiplier (0% = start over)
min_int = 1  # Minimum interval after lapse
leech_action = 0  # 0=suspend, 1=tag only
leech_fails = 8  # Number of lapses before leech
