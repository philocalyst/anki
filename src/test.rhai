fn main(card) {
    // the user just gets the basic features of isolated Rhai + a Regex module + a well-typed card structure
	card.author
    card.tags

    card.fields // array


    // The final card
    return card
}
